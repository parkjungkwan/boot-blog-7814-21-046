spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mariadb
    driverClassName: org.mariadb.jdbc.Driver
    username: root
    password: root
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8080

student:
  list:
    pageSize: 20


